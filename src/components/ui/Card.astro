---
interface Props {
  inHome?: boolean;
}
const { inHome = false } = Astro.props;
const padding = inHome ? "p-8" : "p-2";
---

<article
  class:list={[
    "bg-primary border border-border-gray rounded-lg shadow-lg shadow-black/50",
    padding,
  ]}
>
  <slot />
</article>
